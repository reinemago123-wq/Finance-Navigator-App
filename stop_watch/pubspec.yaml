name: stop_watch
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  vector_math: ^2.2.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  meta: ^1.17.0        # Update this line
  test_api: ^0.7.10    # Update this line
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_icons:
  android: true
  ios: true
  image_path: "assets/stop_watch.png"
  adaptive_icon_background: "#0F172A" # Matches your Pro UI background
  adaptive_icon_foreground: "assets/stop_watch.png"

flutter:

  uses-material-design: true
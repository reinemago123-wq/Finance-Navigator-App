import 'package:flutter/material.dart';

class AppConfig {
  // Colors
  static const Color bgDark = Color(0xFF0F172A);         // Professional Navy
  static const Color primaryOrange = Color(0xFFFF9800);  // Bold Orange
  static const Color accentOrange = Color(0xFFFFB74D);   // Light Orange
  static const Color timerText = Colors.white;
  
  // Dimensions
  static const double clockSize = 280.0;
  static const double clockFontSize = 64.0;
  static const double btnWidth = 260.0;
  static const double btnHeight = 60.0;
  static const double btnSpacing = 20.0;
  static const double btnFontSize = 18.0;
  
  // Effects
  static const double shadowBlur = 15.0;
  static const double shadowOpacity = 0.4;
  static const double borderRadius = 15.0;
}